import { Outlet } from 'react-router-dom'
import { Button } from 'antd'
const Login = () => {
    return (
        <div>
            home
            <Button >a</Button>
            <Outlet />
        </div>
    )
}
export default Login